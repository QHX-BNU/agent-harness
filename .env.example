# demo: deterministic, offline, no key required
HARNESS_PROVIDER=demo

# real model: any OpenAI-compatible Chat Completions endpoint
# HARNESS_PROVIDER=openai-compatible
# OPENAI_API_KEY=replace-me
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=replace-with-an-available-model

# Optional API protection for `python -m agent_harness serve`
# HARNESS_API_KEY=local-secret

